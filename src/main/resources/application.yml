spring:
  application:
    name: study308

  datasource:
    url: jdbc:mysql://localhost:3309/jwbook?serverTimezone=UTC
    username: root
    password: 1111

  jpa:
    hibernate:
      ddl-auto: update

jwt:
  secret: study308-secret-key-must-be-at-least-256-bits!!

  server:
    address: 0.0.0.0
    port: 8080

    springdoc:
      swagger-ui:
        path: /swagger-ui.html
      api-docs:
        path: /v3/api-docs
